# Code generated by fin-protoc. DO NOT EDIT.
from bytebuf import ByteBuf
from checksum import create_checksum_service
from codec import *

class SzseBinary(BinaryCodec):
    def __init__(self):
        self.msg_type = 0
        self.body_length = 0
        self.body = None
        self.checksum = 0
    
    def encode(self, buffer: ByteBuf):
        buffer.write_u32(self.msg_type)
        body_buf = ByteBuf()
        self.body.encode(body_buf)
        self.body_length = body_buf.readable_bytes_len()
        buffer.write_u32(self.body_length)
        buffer.write_bytes(body_buf.to_bytes())
        service = create_checksum_service("SZSE_BIN")
        if service :
            self.checksum = service.calc(buffer)
        buffer.write_i32(self.checksum)
    
    def decode(self, buffer: ByteBuf):
        self.msg_type = buffer.read_u32()
        self.body_length = buffer.read_u32()
        if self.msg_type == 1:
            self.body = Logon()
        if self.msg_type == 2:
            self.body = Logout()
        if self.msg_type == 3:
            self.body = Heartbeat()
        if self.msg_type == 4:
            self.body = BusinessReject()
        if self.msg_type == 5:
            self.body = ReportSynchronization()
        if self.msg_type == 6:
            self.body = PlatformStateInfo()
        if self.msg_type == 7:
            self.body = ReportFinished()
        if self.msg_type == 9:
            self.body = PlatformPartition()
        if self.msg_type == 10:
            self.body = TradingSessionStatus()
        if self.msg_type == 100101:
            self.body = NewOrder()
        if self.msg_type == 100201:
            self.body = NewOrder()
        if self.msg_type == 100301:
            self.body = NewOrder()
        if self.msg_type == 100401:
            self.body = NewOrder()
        if self.msg_type == 100501:
            self.body = NewOrder()
        if self.msg_type == 100601:
            self.body = NewOrder()
        if self.msg_type == 100701:
            self.body = NewOrder()
        if self.msg_type == 101201:
            self.body = NewOrder()
        if self.msg_type == 101301:
            self.body = NewOrder()
        if self.msg_type == 101401:
            self.body = NewOrder()
        if self.msg_type == 101501:
            self.body = NewOrder()
        if self.msg_type == 101601:
            self.body = NewOrder()
        if self.msg_type == 101701:
            self.body = NewOrder()
        if self.msg_type == 101801:
            self.body = NewOrder()
        if self.msg_type == 101901:
            self.body = NewOrder()
        if self.msg_type == 102301:
            self.body = NewOrder()
        if self.msg_type == 102701:
            self.body = NewOrder()
        if self.msg_type == 102801:
            self.body = NewOrder()
        if self.msg_type == 102901:
            self.body = NewOrder()
        if self.msg_type == 103101:
            self.body = NewOrder()
        if self.msg_type == 106301:
            self.body = NewOrder()
        if self.msg_type == 103301:
            self.body = NewOrder()
        if self.msg_type == 103501:
            self.body = NewOrder()
        if self.msg_type == 103701:
            self.body = NewOrder()
        if self.msg_type == 104101:
            self.body = NewOrder()
        if self.msg_type == 104128:
            self.body = NewOrder()
        if self.msg_type == 104701:
            self.body = NewOrder()
        if self.msg_type == 200102:
            self.body = ExecutionConfirm()
        if self.msg_type == 200202:
            self.body = ExecutionConfirm()
        if self.msg_type == 200302:
            self.body = ExecutionConfirm()
        if self.msg_type == 200402:
            self.body = ExecutionConfirm()
        if self.msg_type == 200502:
            self.body = ExecutionConfirm()
        if self.msg_type == 200602:
            self.body = ExecutionConfirm()
        if self.msg_type == 200702:
            self.body = ExecutionConfirm()
        if self.msg_type == 201202:
            self.body = ExecutionConfirm()
        if self.msg_type == 201302:
            self.body = ExecutionConfirm()
        if self.msg_type == 201402:
            self.body = ExecutionConfirm()
        if self.msg_type == 201502:
            self.body = ExecutionConfirm()
        if self.msg_type == 201602:
            self.body = ExecutionConfirm()
        if self.msg_type == 201702:
            self.body = ExecutionConfirm()
        if self.msg_type == 201802:
            self.body = ExecutionConfirm()
        if self.msg_type == 201902:
            self.body = ExecutionConfirm()
        if self.msg_type == 202202:
            self.body = ExecutionConfirm()
        if self.msg_type == 202302:
            self.body = ExecutionConfirm()
        if self.msg_type == 202702:
            self.body = ExecutionConfirm()
        if self.msg_type == 202802:
            self.body = ExecutionConfirm()
        if self.msg_type == 202902:
            self.body = ExecutionConfirm()
        if self.msg_type == 203102:
            self.body = ExecutionConfirm()
        if self.msg_type == 206302:
            self.body = ExecutionConfirm()
        if self.msg_type == 203302:
            self.body = ExecutionConfirm()
        if self.msg_type == 203502:
            self.body = ExecutionConfirm()
        if self.msg_type == 203702:
            self.body = ExecutionConfirm()
        if self.msg_type == 204102:
            self.body = ExecutionConfirm()
        if self.msg_type == 204129:
            self.body = ExecutionConfirm()
        if self.msg_type == 204702:
            self.body = ExecutionConfirm()
        if self.msg_type == 200115:
            self.body = ExecutionReport()
        if self.msg_type == 200215:
            self.body = ExecutionReport()
        if self.msg_type == 200315:
            self.body = ExecutionReport()
        if self.msg_type == 200415:
            self.body = ExecutionReport()
        if self.msg_type == 200515:
            self.body = ExecutionReport()
        if self.msg_type == 200615:
            self.body = ExecutionReport()
        if self.msg_type == 200715:
            self.body = ExecutionReport()
        if self.msg_type == 206315:
            self.body = ExecutionReport()
        if self.msg_type == 203715:
            self.body = ExecutionReport()
        if self.msg_type == 204115:
            self.body = ExecutionReport()
        if self.msg_type == 204130:
            self.body = ExecutionReport()
        if self.msg_type == 190007:
            self.body = OrderCancelRequest()
        if self.msg_type == 290008:
            self.body = CancelReject()
        self.body.decode(buffer)
        self.checksum = buffer.read_i32()
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.msg_type == other.msg_type,
            self.body_length == other.body_length,
            self.body == other.body,
            self.checksum == other.checksum
        ])
        
    


class Logon(BinaryCodec):
    def __init__(self):
        self.sender_comp_id = ''
        self.target_comp_id = ''
        self.heart_btint = 0
        self.password = ''
        self.default_appl_ver_id = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.sender_comp_id, 20)
        write_fixed_string(buffer, self.target_comp_id, 20)
        buffer.write_i32(self.heart_btint)
        write_fixed_string(buffer, self.password, 16)
        write_fixed_string(buffer, self.default_appl_ver_id, 32)
    
    def decode(self, buffer: ByteBuf):
        self.sender_comp_id = buffer.read_bytes(20).decode('utf-8').strip('\x00')
        self.target_comp_id = buffer.read_bytes(20).decode('utf-8').strip('\x00')
        self.heart_btint = buffer.read_i32()
        self.password = buffer.read_bytes(16).decode('utf-8').strip('\x00')
        self.default_appl_ver_id = buffer.read_bytes(32).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.sender_comp_id == other.sender_comp_id,
            self.target_comp_id == other.target_comp_id,
            self.heart_btint == other.heart_btint,
            self.password == other.password,
            self.default_appl_ver_id == other.default_appl_ver_id
        ])
        
    


class Logout(BinaryCodec):
    def __init__(self):
        self.session_status = 0
        self.text = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i32(self.session_status)
        write_fixed_string(buffer, self.text, 200)
    
    def decode(self, buffer: ByteBuf):
        self.session_status = buffer.read_i32()
        self.text = buffer.read_bytes(200).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.session_status == other.session_status,
            self.text == other.text
        ])
        
    


class Heartbeat(BinaryCodec):
    def __init__(self):
        pass
    
    def encode(self, buffer: ByteBuf):
        pass
    
    def decode(self, buffer: ByteBuf):
        pass
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return True
    


class NewOrder(BinaryCodec):
    def __init__(self):
        self.appl_id = ''
        self.submitting_pbuid = ''
        self.security_id = ''
        self.security_id_source = ''
        self.owner_type = 0
        self.clearing_firm = ''
        self.transact_time = 0
        self.user_info = ''
        self.cl_ord_id = ''
        self.account_id = ''
        self.branch_id = ''
        self.order_restrictions = ''
        self.side = ''
        self.ord_type = ''
        self.order_qty = 0
        self.price = 0
        self.appl_extend = None
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.appl_id, 3)
        write_fixed_string(buffer, self.submitting_pbuid, 6)
        write_fixed_string(buffer, self.security_id, 8)
        write_fixed_string(buffer, self.security_id_source, 4)
        buffer.write_u16(self.owner_type)
        write_fixed_string(buffer, self.clearing_firm, 2)
        buffer.write_i64(self.transact_time)
        write_fixed_string(buffer, self.user_info, 8)
        write_fixed_string(buffer, self.cl_ord_id, 10)
        write_fixed_string(buffer, self.account_id, 12)
        write_fixed_string(buffer, self.branch_id, 4)
        write_fixed_string(buffer, self.order_restrictions, 4)
        write_fixed_string(buffer, self.side, 1)
        write_fixed_string(buffer, self.ord_type, 1)
        buffer.write_i64(self.order_qty)
        buffer.write_i64(self.price)
        if self.appl_extend is not None:
            self.appl_extend.encode(buffer)
    
    def decode(self, buffer: ByteBuf):
        self.appl_id = buffer.read_bytes(3).decode('utf-8').strip('\x00')
        self.submitting_pbuid = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.security_id = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.security_id_source = buffer.read_bytes(4).decode('utf-8').strip('\x00')
        self.owner_type = buffer.read_u16()
        self.clearing_firm = buffer.read_bytes(2).decode('utf-8').strip('\x00')
        self.transact_time = buffer.read_i64()
        self.user_info = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.cl_ord_id = buffer.read_bytes(10).decode('utf-8').strip('\x00')
        self.account_id = buffer.read_bytes(12).decode('utf-8').strip('\x00')
        self.branch_id = buffer.read_bytes(4).decode('utf-8').strip('\x00')
        self.order_restrictions = buffer.read_bytes(4).decode('utf-8').strip('\x00')
        self.side = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.ord_type = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.order_qty = buffer.read_i64()
        self.price = buffer.read_i64()
        if self.appl_id == "010":
            self.appl_extend = Extend100101()
        if self.appl_id == "020":
            self.appl_extend = Extend100201()
        if self.appl_id == "030":
            self.appl_extend = Extend100301()
        if self.appl_id == "051":
            self.appl_extend = Extend100501()
        if self.appl_id == "052":
            self.appl_extend = Extend100501()
        if self.appl_id == "060":
            self.appl_extend = Extend100601()
        if self.appl_id == "061":
            self.appl_extend = Extend100601()
        if self.appl_id == "070":
            self.appl_extend = Extend100701()
        if self.appl_id == "150":
            self.appl_extend = Extend101501()
        if self.appl_id == "151":
            self.appl_extend = Extend101501()
        if self.appl_id == "152":
            self.appl_extend = Extend101501()
        if self.appl_id == "160":
            self.appl_extend = Extend101601()
        if self.appl_id == "170":
            self.appl_extend = Extend101701()
        if self.appl_id == "180":
            self.appl_extend = Extend101801()
        if self.appl_id == "181":
            self.appl_extend = Extend101801()
        if self.appl_id == "270":
            self.appl_extend = Extend102701()
        if self.appl_id == "271":
            self.appl_extend = Extend102701()
        if self.appl_id == "280":
            self.appl_extend = Extend102801()
        if self.appl_id == "281":
            self.appl_extend = Extend102801()
        if self.appl_id == "290":
            self.appl_extend = Extend102901()
        if self.appl_id == "291":
            self.appl_extend = Extend102901()
        if self.appl_id == "630":
            self.appl_extend = Extend106301()
        if self.appl_id == "350":
            self.appl_extend = Extend103501()
        if self.appl_id == "351":
            self.appl_extend = Extend103501()
        if self.appl_id == "370":
            self.appl_extend = Extend103701()
        if self.appl_id == "410":
            self.appl_extend = Extend104101()
        if self.appl_id == "417":
            self.appl_extend = Extend104128()
        if self.appl_id == "470":
            self.appl_extend = Extend104701()
        self.appl_extend.decode(buffer)
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.appl_id == other.appl_id,
            self.submitting_pbuid == other.submitting_pbuid,
            self.security_id == other.security_id,
            self.security_id_source == other.security_id_source,
            self.owner_type == other.owner_type,
            self.clearing_firm == other.clearing_firm,
            self.transact_time == other.transact_time,
            self.user_info == other.user_info,
            self.cl_ord_id == other.cl_ord_id,
            self.account_id == other.account_id,
            self.branch_id == other.branch_id,
            self.order_restrictions == other.order_restrictions,
            self.side == other.side,
            self.ord_type == other.ord_type,
            self.order_qty == other.order_qty,
            self.price == other.price,
            self.appl_extend == other.appl_extend
        ])
        
    


class Extend100101(BinaryCodec):
    def __init__(self):
        self.stop_px = 0
        self.min_qty = 0
        self.max_price_levels = 0
        self.time_in_force = ''
        self.cash_margin = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i64(self.stop_px)
        buffer.write_i64(self.min_qty)
        buffer.write_u16(self.max_price_levels)
        write_fixed_string(buffer, self.time_in_force, 1)
        write_fixed_string(buffer, self.cash_margin, 1)
    
    def decode(self, buffer: ByteBuf):
        self.stop_px = buffer.read_i64()
        self.min_qty = buffer.read_i64()
        self.max_price_levels = buffer.read_u16()
        self.time_in_force = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.stop_px == other.stop_px,
            self.min_qty == other.min_qty,
            self.max_price_levels == other.max_price_levels,
            self.time_in_force == other.time_in_force,
            self.cash_margin == other.cash_margin
        ])
        
    


class Extend100201(BinaryCodec):
    def __init__(self):
        self.stop_px = 0
        self.min_qty = 0
        self.max_price_levels = 0
        self.time_in_force = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i64(self.stop_px)
        buffer.write_i64(self.min_qty)
        buffer.write_u16(self.max_price_levels)
        write_fixed_string(buffer, self.time_in_force, 1)
    
    def decode(self, buffer: ByteBuf):
        self.stop_px = buffer.read_i64()
        self.min_qty = buffer.read_i64()
        self.max_price_levels = buffer.read_u16()
        self.time_in_force = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.stop_px == other.stop_px,
            self.min_qty == other.min_qty,
            self.max_price_levels == other.max_price_levels,
            self.time_in_force == other.time_in_force
        ])
        
    


class Extend100301(BinaryCodec):
    def __init__(self):
        self.stop_px = 0
        self.min_qty = 0
        self.max_price_levels = 0
        self.time_in_force = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i64(self.stop_px)
        buffer.write_i64(self.min_qty)
        buffer.write_u16(self.max_price_levels)
        write_fixed_string(buffer, self.time_in_force, 1)
    
    def decode(self, buffer: ByteBuf):
        self.stop_px = buffer.read_i64()
        self.min_qty = buffer.read_i64()
        self.max_price_levels = buffer.read_u16()
        self.time_in_force = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.stop_px == other.stop_px,
            self.min_qty == other.min_qty,
            self.max_price_levels == other.max_price_levels,
            self.time_in_force == other.time_in_force
        ])
        
    


class Extend101401(BinaryCodec):
    def __init__(self):
        self.stop_px = 0
        self.min_qty = 0
        self.max_price_levels = 0
        self.time_in_force = ''
        self.position_effect = ''
        self.covered_or_uncovered = 0
        self.contract_account_code = ''
        self.secondary_order_id = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i64(self.stop_px)
        buffer.write_i64(self.min_qty)
        buffer.write_u16(self.max_price_levels)
        write_fixed_string(buffer, self.time_in_force, 1)
        write_fixed_string(buffer, self.position_effect, 1)
        buffer.write_u8(self.covered_or_uncovered)
        write_fixed_string(buffer, self.contract_account_code, 6)
        write_fixed_string(buffer, self.secondary_order_id, 16)
    
    def decode(self, buffer: ByteBuf):
        self.stop_px = buffer.read_i64()
        self.min_qty = buffer.read_i64()
        self.max_price_levels = buffer.read_u16()
        self.time_in_force = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.position_effect = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.covered_or_uncovered = buffer.read_u8()
        self.contract_account_code = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.secondary_order_id = buffer.read_bytes(16).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.stop_px == other.stop_px,
            self.min_qty == other.min_qty,
            self.max_price_levels == other.max_price_levels,
            self.time_in_force == other.time_in_force,
            self.position_effect == other.position_effect,
            self.covered_or_uncovered == other.covered_or_uncovered,
            self.contract_account_code == other.contract_account_code,
            self.secondary_order_id == other.secondary_order_id
        ])
        
    


class Extend100501(BinaryCodec):
    def __init__(self):
        self.confirm_id = ''
        self.cash_margin = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.confirm_id, 8)
        write_fixed_string(buffer, self.cash_margin, 1)
    
    def decode(self, buffer: ByteBuf):
        self.confirm_id = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.confirm_id == other.confirm_id,
            self.cash_margin == other.cash_margin
        ])
        
    


class Extend100601(BinaryCodec):
    def __init__(self):
        self.cash_margin = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.cash_margin, 1)
    
    def decode(self, buffer: ByteBuf):
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.cash_margin == other.cash_margin
        ])
        
    


class Extend100701(BinaryCodec):
    def __init__(self):
        self.expiration_days = 0
        self.expiration_type = 0
        self.share_property = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_u16(self.expiration_days)
        buffer.write_u8(self.expiration_type)
        write_fixed_string(buffer, self.share_property, 2)
    
    def decode(self, buffer: ByteBuf):
        self.expiration_days = buffer.read_u16()
        self.expiration_type = buffer.read_u8()
        self.share_property = buffer.read_bytes(2).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.expiration_days == other.expiration_days,
            self.expiration_type == other.expiration_type,
            self.share_property == other.share_property
        ])
        
    


class Extend101501(BinaryCodec):
    def __init__(self):
        self.share_property = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.share_property, 2)
    
    def decode(self, buffer: ByteBuf):
        self.share_property = buffer.read_bytes(2).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.share_property == other.share_property
        ])
        
    


class Extend101601(BinaryCodec):
    def __init__(self):
        self.contract_account_code = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.contract_account_code, 6)
    
    def decode(self, buffer: ByteBuf):
        self.contract_account_code = buffer.read_bytes(6).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.contract_account_code == other.contract_account_code
        ])
        
    


class Extend101701(BinaryCodec):
    def __init__(self):
        self.cash_order_qty = 0
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i64(self.cash_order_qty)
    
    def decode(self, buffer: ByteBuf):
        self.cash_order_qty = buffer.read_i64()
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.cash_order_qty == other.cash_order_qty
        ])
        
    


class Extend101801(BinaryCodec):
    def __init__(self):
        self.tenderer = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.tenderer, 6)
    
    def decode(self, buffer: ByteBuf):
        self.tenderer = buffer.read_bytes(6).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.tenderer == other.tenderer
        ])
        
    


class Extend102701(BinaryCodec):
    def __init__(self):
        self.disposal_pbu = ''
        self.disposal_account_id = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.disposal_pbu, 6)
        write_fixed_string(buffer, self.disposal_account_id, 12)
    
    def decode(self, buffer: ByteBuf):
        self.disposal_pbu = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.disposal_account_id = buffer.read_bytes(12).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.disposal_pbu == other.disposal_pbu,
            self.disposal_account_id == other.disposal_account_id
        ])
        
    


class Extend102801(BinaryCodec):
    def __init__(self):
        self.lender_pbu = ''
        self.lender_account_id = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.lender_pbu, 6)
        write_fixed_string(buffer, self.lender_account_id, 12)
    
    def decode(self, buffer: ByteBuf):
        self.lender_pbu = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.lender_account_id = buffer.read_bytes(12).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.lender_pbu == other.lender_pbu,
            self.lender_account_id == other.lender_account_id
        ])
        
    


class Extend102901(BinaryCodec):
    def __init__(self):
        self.deduction_pbu = ''
        self.deduction_account_id = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.deduction_pbu, 6)
        write_fixed_string(buffer, self.deduction_account_id, 12)
    
    def decode(self, buffer: ByteBuf):
        self.deduction_pbu = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.deduction_account_id = buffer.read_bytes(12).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.deduction_pbu == other.deduction_pbu,
            self.deduction_account_id == other.deduction_account_id
        ])
        
    


class Extend106301(BinaryCodec):
    def __init__(self):
        self.stop_px = 0
        self.min_qty = 0
        self.max_price_levels = 0
        self.time_in_force = ''
        self.lot_type = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i64(self.stop_px)
        buffer.write_i64(self.min_qty)
        buffer.write_u16(self.max_price_levels)
        write_fixed_string(buffer, self.time_in_force, 1)
        write_fixed_string(buffer, self.lot_type, 1)
    
    def decode(self, buffer: ByteBuf):
        self.stop_px = buffer.read_i64()
        self.min_qty = buffer.read_i64()
        self.max_price_levels = buffer.read_u16()
        self.time_in_force = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.lot_type = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.stop_px == other.stop_px,
            self.min_qty == other.min_qty,
            self.max_price_levels == other.max_price_levels,
            self.time_in_force == other.time_in_force,
            self.lot_type == other.lot_type
        ])
        
    


class Extend103501(BinaryCodec):
    def __init__(self):
        self.contract_account_code = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.contract_account_code, 6)
    
    def decode(self, buffer: ByteBuf):
        self.contract_account_code = buffer.read_bytes(6).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.contract_account_code == other.contract_account_code
        ])
        
    


class Extend103701(BinaryCodec):
    def __init__(self):
        self.cash_margin = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.cash_margin, 1)
    
    def decode(self, buffer: ByteBuf):
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.cash_margin == other.cash_margin
        ])
        
    


class Extend104101(BinaryCodec):
    def __init__(self):
        self.stop_px = 0
        self.min_qty = 0
        self.max_price_levels = 0
        self.time_in_force = ''
        self.cash_margin = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i64(self.stop_px)
        buffer.write_i64(self.min_qty)
        buffer.write_u16(self.max_price_levels)
        write_fixed_string(buffer, self.time_in_force, 1)
        write_fixed_string(buffer, self.cash_margin, 1)
    
    def decode(self, buffer: ByteBuf):
        self.stop_px = buffer.read_i64()
        self.min_qty = buffer.read_i64()
        self.max_price_levels = buffer.read_u16()
        self.time_in_force = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.stop_px == other.stop_px,
            self.min_qty == other.min_qty,
            self.max_price_levels == other.max_price_levels,
            self.time_in_force == other.time_in_force,
            self.cash_margin == other.cash_margin
        ])
        
    


class Extend104128(BinaryCodec):
    def __init__(self):
        self.member_id = ''
        self.investor_type = ''
        self.investor_id = ''
        self.investor_name = ''
        self.trader_code = ''
        self.secondary_order_id = ''
        self.bid_trans_type = 0
        self.bid_exec_inst_type = 0
        self.low_limit_price = 0
        self.high_limit_price = 0
        self.min_qty = 0
        self.trade_date = 0
        self.settl_type = 0
        self.settl_period = 0
        self.pre_trade_anonymity = 0
        self.cash_margin = ''
        self.memo = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.member_id, 6)
        write_fixed_string(buffer, self.investor_type, 2)
        write_fixed_string(buffer, self.investor_id, 10)
        write_fixed_string(buffer, self.investor_name, 120)
        write_fixed_string(buffer, self.trader_code, 8)
        write_fixed_string(buffer, self.secondary_order_id, 16)
        buffer.write_u16(self.bid_trans_type)
        buffer.write_u16(self.bid_exec_inst_type)
        buffer.write_i64(self.low_limit_price)
        buffer.write_i64(self.high_limit_price)
        buffer.write_i64(self.min_qty)
        buffer.write_u32(self.trade_date)
        buffer.write_u16(self.settl_type)
        buffer.write_u8(self.settl_period)
        buffer.write_u8(self.pre_trade_anonymity)
        write_fixed_string(buffer, self.cash_margin, 1)
        write_fixed_string(buffer, self.memo, 160)
    
    def decode(self, buffer: ByteBuf):
        self.member_id = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.investor_type = buffer.read_bytes(2).decode('utf-8').strip('\x00')
        self.investor_id = buffer.read_bytes(10).decode('utf-8').strip('\x00')
        self.investor_name = buffer.read_bytes(120).decode('utf-8').strip('\x00')
        self.trader_code = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.secondary_order_id = buffer.read_bytes(16).decode('utf-8').strip('\x00')
        self.bid_trans_type = buffer.read_u16()
        self.bid_exec_inst_type = buffer.read_u16()
        self.low_limit_price = buffer.read_i64()
        self.high_limit_price = buffer.read_i64()
        self.min_qty = buffer.read_i64()
        self.trade_date = buffer.read_u32()
        self.settl_type = buffer.read_u16()
        self.settl_period = buffer.read_u8()
        self.pre_trade_anonymity = buffer.read_u8()
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.memo = buffer.read_bytes(160).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.member_id == other.member_id,
            self.investor_type == other.investor_type,
            self.investor_id == other.investor_id,
            self.investor_name == other.investor_name,
            self.trader_code == other.trader_code,
            self.secondary_order_id == other.secondary_order_id,
            self.bid_trans_type == other.bid_trans_type,
            self.bid_exec_inst_type == other.bid_exec_inst_type,
            self.low_limit_price == other.low_limit_price,
            self.high_limit_price == other.high_limit_price,
            self.min_qty == other.min_qty,
            self.trade_date == other.trade_date,
            self.settl_type == other.settl_type,
            self.settl_period == other.settl_period,
            self.pre_trade_anonymity == other.pre_trade_anonymity,
            self.cash_margin == other.cash_margin,
            self.memo == other.memo
        ])
        
    


class Extend104701(BinaryCodec):
    def __init__(self):
        self.secondary_order_id = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.secondary_order_id, 16)
    
    def decode(self, buffer: ByteBuf):
        self.secondary_order_id = buffer.read_bytes(16).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.secondary_order_id == other.secondary_order_id
        ])
        
    


class ExecutionConfirm(BinaryCodec):
    def __init__(self):
        self.partition_no = 0
        self.report_index = 0
        self.appl_id = ''
        self.reporting_pbuid = ''
        self.submitting_pbuid = ''
        self.security_id = ''
        self.security_id_source = ''
        self.owner_type = 0
        self.clearing_firm = ''
        self.transact_time = 0
        self.user_info = ''
        self.order_id = ''
        self.cl_ord_id = ''
        self.quote_msg_id = ''
        self.orig_cl_ord_id = ''
        self.exec_id = ''
        self.exec_type = ''
        self.ord_status = ''
        self.ord_rej_reason = 0
        self.leaves_qty = 0
        self.cum_qty = 0
        self.side = ''
        self.ord_type = ''
        self.order_qty = 0
        self.price = 0
        self.account_id = ''
        self.branch_id = ''
        self.order_restrictions = ''
        self.appl_extend = None
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i32(self.partition_no)
        buffer.write_i64(self.report_index)
        write_fixed_string(buffer, self.appl_id, 3)
        write_fixed_string(buffer, self.reporting_pbuid, 6)
        write_fixed_string(buffer, self.submitting_pbuid, 6)
        write_fixed_string(buffer, self.security_id, 8)
        write_fixed_string(buffer, self.security_id_source, 4)
        buffer.write_u16(self.owner_type)
        write_fixed_string(buffer, self.clearing_firm, 2)
        buffer.write_i64(self.transact_time)
        write_fixed_string(buffer, self.user_info, 8)
        write_fixed_string(buffer, self.order_id, 16)
        write_fixed_string(buffer, self.cl_ord_id, 10)
        write_fixed_string(buffer, self.quote_msg_id, 10)
        write_fixed_string(buffer, self.orig_cl_ord_id, 10)
        write_fixed_string(buffer, self.exec_id, 16)
        write_fixed_string(buffer, self.exec_type, 1)
        write_fixed_string(buffer, self.ord_status, 1)
        buffer.write_u16(self.ord_rej_reason)
        buffer.write_i64(self.leaves_qty)
        buffer.write_i64(self.cum_qty)
        write_fixed_string(buffer, self.side, 1)
        write_fixed_string(buffer, self.ord_type, 1)
        buffer.write_i64(self.order_qty)
        buffer.write_i64(self.price)
        write_fixed_string(buffer, self.account_id, 12)
        write_fixed_string(buffer, self.branch_id, 4)
        write_fixed_string(buffer, self.order_restrictions, 4)
        if self.appl_extend is not None:
            self.appl_extend.encode(buffer)
    
    def decode(self, buffer: ByteBuf):
        self.partition_no = buffer.read_i32()
        self.report_index = buffer.read_i64()
        self.appl_id = buffer.read_bytes(3).decode('utf-8').strip('\x00')
        self.reporting_pbuid = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.submitting_pbuid = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.security_id = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.security_id_source = buffer.read_bytes(4).decode('utf-8').strip('\x00')
        self.owner_type = buffer.read_u16()
        self.clearing_firm = buffer.read_bytes(2).decode('utf-8').strip('\x00')
        self.transact_time = buffer.read_i64()
        self.user_info = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.order_id = buffer.read_bytes(16).decode('utf-8').strip('\x00')
        self.cl_ord_id = buffer.read_bytes(10).decode('utf-8').strip('\x00')
        self.quote_msg_id = buffer.read_bytes(10).decode('utf-8').strip('\x00')
        self.orig_cl_ord_id = buffer.read_bytes(10).decode('utf-8').strip('\x00')
        self.exec_id = buffer.read_bytes(16).decode('utf-8').strip('\x00')
        self.exec_type = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.ord_status = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.ord_rej_reason = buffer.read_u16()
        self.leaves_qty = buffer.read_i64()
        self.cum_qty = buffer.read_i64()
        self.side = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.ord_type = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.order_qty = buffer.read_i64()
        self.price = buffer.read_i64()
        self.account_id = buffer.read_bytes(12).decode('utf-8').strip('\x00')
        self.branch_id = buffer.read_bytes(4).decode('utf-8').strip('\x00')
        self.order_restrictions = buffer.read_bytes(4).decode('utf-8').strip('\x00')
        if self.appl_id == "010":
            self.appl_extend = Extend200102()
        if self.appl_id == "020":
            self.appl_extend = Extend200202()
        if self.appl_id == "030":
            self.appl_extend = Extend200302()
        if self.appl_id == "051":
            self.appl_extend = Extend200502()
        if self.appl_id == "052":
            self.appl_extend = Extend200502()
        if self.appl_id == "060":
            self.appl_extend = Extend200602()
        if self.appl_id == "061":
            self.appl_extend = Extend200602()
        if self.appl_id == "070":
            self.appl_extend = Extend200702()
        if self.appl_id == "150":
            self.appl_extend = Extend201502()
        if self.appl_id == "151":
            self.appl_extend = Extend201502()
        if self.appl_id == "152":
            self.appl_extend = Extend201502()
        if self.appl_id == "160":
            self.appl_extend = Extend201602()
        if self.appl_id == "170":
            self.appl_extend = Extend201702()
        if self.appl_id == "180":
            self.appl_extend = Extend201802()
        if self.appl_id == "181":
            self.appl_extend = Extend201802()
        if self.appl_id == "270":
            self.appl_extend = Extend202702()
        if self.appl_id == "271":
            self.appl_extend = Extend202702()
        if self.appl_id == "280":
            self.appl_extend = Extend202802()
        if self.appl_id == "281":
            self.appl_extend = Extend202802()
        if self.appl_id == "290":
            self.appl_extend = Extend202902()
        if self.appl_id == "291":
            self.appl_extend = Extend202902()
        if self.appl_id == "630":
            self.appl_extend = Extend206302()
        if self.appl_id == "350":
            self.appl_extend = Extend203502()
        if self.appl_id == "351":
            self.appl_extend = Extend203502()
        if self.appl_id == "370":
            self.appl_extend = Extend203702()
        if self.appl_id == "410":
            self.appl_extend = Extend204102()
        if self.appl_id == "417":
            self.appl_extend = Extend204129()
        if self.appl_id == "470":
            self.appl_extend = Extend204702()
        self.appl_extend.decode(buffer)
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.partition_no == other.partition_no,
            self.report_index == other.report_index,
            self.appl_id == other.appl_id,
            self.reporting_pbuid == other.reporting_pbuid,
            self.submitting_pbuid == other.submitting_pbuid,
            self.security_id == other.security_id,
            self.security_id_source == other.security_id_source,
            self.owner_type == other.owner_type,
            self.clearing_firm == other.clearing_firm,
            self.transact_time == other.transact_time,
            self.user_info == other.user_info,
            self.order_id == other.order_id,
            self.cl_ord_id == other.cl_ord_id,
            self.quote_msg_id == other.quote_msg_id,
            self.orig_cl_ord_id == other.orig_cl_ord_id,
            self.exec_id == other.exec_id,
            self.exec_type == other.exec_type,
            self.ord_status == other.ord_status,
            self.ord_rej_reason == other.ord_rej_reason,
            self.leaves_qty == other.leaves_qty,
            self.cum_qty == other.cum_qty,
            self.side == other.side,
            self.ord_type == other.ord_type,
            self.order_qty == other.order_qty,
            self.price == other.price,
            self.account_id == other.account_id,
            self.branch_id == other.branch_id,
            self.order_restrictions == other.order_restrictions,
            self.appl_extend == other.appl_extend
        ])
        
    


class Extend200102(BinaryCodec):
    def __init__(self):
        self.stop_px = 0
        self.min_qty = 0
        self.max_price_levels = 0
        self.time_in_force = ''
        self.cash_margin = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i64(self.stop_px)
        buffer.write_i64(self.min_qty)
        buffer.write_u16(self.max_price_levels)
        write_fixed_string(buffer, self.time_in_force, 1)
        write_fixed_string(buffer, self.cash_margin, 1)
    
    def decode(self, buffer: ByteBuf):
        self.stop_px = buffer.read_i64()
        self.min_qty = buffer.read_i64()
        self.max_price_levels = buffer.read_u16()
        self.time_in_force = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.stop_px == other.stop_px,
            self.min_qty == other.min_qty,
            self.max_price_levels == other.max_price_levels,
            self.time_in_force == other.time_in_force,
            self.cash_margin == other.cash_margin
        ])
        
    


class Extend200202(BinaryCodec):
    def __init__(self):
        self.stop_px = 0
        self.min_qty = 0
        self.max_price_levels = 0
        self.time_in_force = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i64(self.stop_px)
        buffer.write_i64(self.min_qty)
        buffer.write_u16(self.max_price_levels)
        write_fixed_string(buffer, self.time_in_force, 1)
    
    def decode(self, buffer: ByteBuf):
        self.stop_px = buffer.read_i64()
        self.min_qty = buffer.read_i64()
        self.max_price_levels = buffer.read_u16()
        self.time_in_force = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.stop_px == other.stop_px,
            self.min_qty == other.min_qty,
            self.max_price_levels == other.max_price_levels,
            self.time_in_force == other.time_in_force
        ])
        
    


class Extend200302(BinaryCodec):
    def __init__(self):
        self.stop_px = 0
        self.min_qty = 0
        self.max_price_levels = 0
        self.time_in_force = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i64(self.stop_px)
        buffer.write_i64(self.min_qty)
        buffer.write_u16(self.max_price_levels)
        write_fixed_string(buffer, self.time_in_force, 1)
    
    def decode(self, buffer: ByteBuf):
        self.stop_px = buffer.read_i64()
        self.min_qty = buffer.read_i64()
        self.max_price_levels = buffer.read_u16()
        self.time_in_force = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.stop_px == other.stop_px,
            self.min_qty == other.min_qty,
            self.max_price_levels == other.max_price_levels,
            self.time_in_force == other.time_in_force
        ])
        
    


class Extend200402(BinaryCodec):
    def __init__(self):
        self.stop_px = 0
        self.min_qty = 0
        self.max_price_levels = 0
        self.time_in_force = ''
        self.position_effect = ''
        self.covered_or_uncovered = 0
        self.contract_account_code = ''
        self.secondary_order_id = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i64(self.stop_px)
        buffer.write_i64(self.min_qty)
        buffer.write_u16(self.max_price_levels)
        write_fixed_string(buffer, self.time_in_force, 1)
        write_fixed_string(buffer, self.position_effect, 1)
        buffer.write_u8(self.covered_or_uncovered)
        write_fixed_string(buffer, self.contract_account_code, 6)
        write_fixed_string(buffer, self.secondary_order_id, 16)
    
    def decode(self, buffer: ByteBuf):
        self.stop_px = buffer.read_i64()
        self.min_qty = buffer.read_i64()
        self.max_price_levels = buffer.read_u16()
        self.time_in_force = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.position_effect = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.covered_or_uncovered = buffer.read_u8()
        self.contract_account_code = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.secondary_order_id = buffer.read_bytes(16).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.stop_px == other.stop_px,
            self.min_qty == other.min_qty,
            self.max_price_levels == other.max_price_levels,
            self.time_in_force == other.time_in_force,
            self.position_effect == other.position_effect,
            self.covered_or_uncovered == other.covered_or_uncovered,
            self.contract_account_code == other.contract_account_code,
            self.secondary_order_id == other.secondary_order_id
        ])
        
    


class Extend200502(BinaryCodec):
    def __init__(self):
        self.confirm_id = ''
        self.cash_margin = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.confirm_id, 8)
        write_fixed_string(buffer, self.cash_margin, 1)
    
    def decode(self, buffer: ByteBuf):
        self.confirm_id = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.confirm_id == other.confirm_id,
            self.cash_margin == other.cash_margin
        ])
        
    


class Extend200602(BinaryCodec):
    def __init__(self):
        self.cash_margin = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.cash_margin, 1)
    
    def decode(self, buffer: ByteBuf):
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.cash_margin == other.cash_margin
        ])
        
    


class Extend200702(BinaryCodec):
    def __init__(self):
        self.expiration_days = 0
        self.expiration_type = 0
        self.share_property = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_u16(self.expiration_days)
        buffer.write_u8(self.expiration_type)
        write_fixed_string(buffer, self.share_property, 2)
    
    def decode(self, buffer: ByteBuf):
        self.expiration_days = buffer.read_u16()
        self.expiration_type = buffer.read_u8()
        self.share_property = buffer.read_bytes(2).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.expiration_days == other.expiration_days,
            self.expiration_type == other.expiration_type,
            self.share_property == other.share_property
        ])
        
    


class Extend201202(BinaryCodec):
    def __init__(self):
        self.insufficient_security_id = ''
        self.no_security = 0
        self.underlying_security_id = ''
        self.underlying_security_id_source = ''
        self.delivery_qty = 0
        self.subst_cash = 0
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.insufficient_security_id, 8)
        buffer.write_u32(self.no_security)
        write_fixed_string(buffer, self.underlying_security_id, 8)
        write_fixed_string(buffer, self.underlying_security_id_source, 4)
        buffer.write_i64(self.delivery_qty)
        buffer.write_i64(self.subst_cash)
    
    def decode(self, buffer: ByteBuf):
        self.insufficient_security_id = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.no_security = buffer.read_u32()
        self.underlying_security_id = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.underlying_security_id_source = buffer.read_bytes(4).decode('utf-8').strip('\x00')
        self.delivery_qty = buffer.read_i64()
        self.subst_cash = buffer.read_i64()
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.insufficient_security_id == other.insufficient_security_id,
            self.no_security == other.no_security,
            self.underlying_security_id == other.underlying_security_id,
            self.underlying_security_id_source == other.underlying_security_id_source,
            self.delivery_qty == other.delivery_qty,
            self.subst_cash == other.subst_cash
        ])
        
    


class Extend201502(BinaryCodec):
    def __init__(self):
        self.share_property = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.share_property, 2)
    
    def decode(self, buffer: ByteBuf):
        self.share_property = buffer.read_bytes(2).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.share_property == other.share_property
        ])
        
    


class Extend201602(BinaryCodec):
    def __init__(self):
        self.contract_account_code = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.contract_account_code, 6)
    
    def decode(self, buffer: ByteBuf):
        self.contract_account_code = buffer.read_bytes(6).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.contract_account_code == other.contract_account_code
        ])
        
    


class Extend201702(BinaryCodec):
    def __init__(self):
        self.cash_order_qty = 0
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i64(self.cash_order_qty)
    
    def decode(self, buffer: ByteBuf):
        self.cash_order_qty = buffer.read_i64()
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.cash_order_qty == other.cash_order_qty
        ])
        
    


class Extend201802(BinaryCodec):
    def __init__(self):
        self.tenderer = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.tenderer, 6)
    
    def decode(self, buffer: ByteBuf):
        self.tenderer = buffer.read_bytes(6).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.tenderer == other.tenderer
        ])
        
    


class Extend202702(BinaryCodec):
    def __init__(self):
        self.disposal_pbu = ''
        self.disposal_account_id = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.disposal_pbu, 6)
        write_fixed_string(buffer, self.disposal_account_id, 12)
    
    def decode(self, buffer: ByteBuf):
        self.disposal_pbu = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.disposal_account_id = buffer.read_bytes(12).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.disposal_pbu == other.disposal_pbu,
            self.disposal_account_id == other.disposal_account_id
        ])
        
    


class Extend202802(BinaryCodec):
    def __init__(self):
        self.lender_pbu = ''
        self.lender_account_id = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.lender_pbu, 6)
        write_fixed_string(buffer, self.lender_account_id, 12)
    
    def decode(self, buffer: ByteBuf):
        self.lender_pbu = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.lender_account_id = buffer.read_bytes(12).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.lender_pbu == other.lender_pbu,
            self.lender_account_id == other.lender_account_id
        ])
        
    


class Extend202902(BinaryCodec):
    def __init__(self):
        self.deduction_pbu = ''
        self.deduction_account_id = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.deduction_pbu, 6)
        write_fixed_string(buffer, self.deduction_account_id, 12)
    
    def decode(self, buffer: ByteBuf):
        self.deduction_pbu = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.deduction_account_id = buffer.read_bytes(12).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.deduction_pbu == other.deduction_pbu,
            self.deduction_account_id == other.deduction_account_id
        ])
        
    


class Extend203102(BinaryCodec):
    def __init__(self):
        self.insufficient_security_id = ''
        self.no_security = 0
        self.underlying_security_id = ''
        self.underlying_security_id_source = ''
        self.delivery_qty = 0
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.insufficient_security_id, 8)
        buffer.write_u32(self.no_security)
        write_fixed_string(buffer, self.underlying_security_id, 8)
        write_fixed_string(buffer, self.underlying_security_id_source, 4)
        buffer.write_i64(self.delivery_qty)
    
    def decode(self, buffer: ByteBuf):
        self.insufficient_security_id = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.no_security = buffer.read_u32()
        self.underlying_security_id = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.underlying_security_id_source = buffer.read_bytes(4).decode('utf-8').strip('\x00')
        self.delivery_qty = buffer.read_i64()
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.insufficient_security_id == other.insufficient_security_id,
            self.no_security == other.no_security,
            self.underlying_security_id == other.underlying_security_id,
            self.underlying_security_id_source == other.underlying_security_id_source,
            self.delivery_qty == other.delivery_qty
        ])
        
    


class Extend206302(BinaryCodec):
    def __init__(self):
        self.reject_text = ''
        self.stop_px = 0
        self.min_qty = 0
        self.max_price_levels = 0
        self.time_in_force = ''
        self.lot_type = ''
        self.imc_reject_text_len = 0
        self.imc_reject_text = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.reject_text, 16)
        buffer.write_i64(self.stop_px)
        buffer.write_i64(self.min_qty)
        buffer.write_u16(self.max_price_levels)
        write_fixed_string(buffer, self.time_in_force, 1)
        write_fixed_string(buffer, self.lot_type, 1)
        buffer.write_u32(self.imc_reject_text_len)
        put_string(buffer, self.imc_reject_text, 'u32')
    
    def decode(self, buffer: ByteBuf):
        self.reject_text = buffer.read_bytes(16).decode('utf-8').strip('\x00')
        self.stop_px = buffer.read_i64()
        self.min_qty = buffer.read_i64()
        self.max_price_levels = buffer.read_u16()
        self.time_in_force = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.lot_type = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.imc_reject_text_len = buffer.read_u32()
        self.imc_reject_text = get_string(buffer,'u32')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.reject_text == other.reject_text,
            self.stop_px == other.stop_px,
            self.min_qty == other.min_qty,
            self.max_price_levels == other.max_price_levels,
            self.time_in_force == other.time_in_force,
            self.lot_type == other.lot_type,
            self.imc_reject_text_len == other.imc_reject_text_len,
            self.imc_reject_text == other.imc_reject_text
        ])
        
    


class Extend203502(BinaryCodec):
    def __init__(self):
        self.contract_account_code = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.contract_account_code, 6)
    
    def decode(self, buffer: ByteBuf):
        self.contract_account_code = buffer.read_bytes(6).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.contract_account_code == other.contract_account_code
        ])
        
    


class Extend203702(BinaryCodec):
    def __init__(self):
        self.cash_margin = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.cash_margin, 1)
    
    def decode(self, buffer: ByteBuf):
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.cash_margin == other.cash_margin
        ])
        
    


class Extend204102(BinaryCodec):
    def __init__(self):
        self.stop_px = 0
        self.min_qty = 0
        self.max_price_levels = 0
        self.time_in_force = ''
        self.cash_margin = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i64(self.stop_px)
        buffer.write_i64(self.min_qty)
        buffer.write_u16(self.max_price_levels)
        write_fixed_string(buffer, self.time_in_force, 1)
        write_fixed_string(buffer, self.cash_margin, 1)
    
    def decode(self, buffer: ByteBuf):
        self.stop_px = buffer.read_i64()
        self.min_qty = buffer.read_i64()
        self.max_price_levels = buffer.read_u16()
        self.time_in_force = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.stop_px == other.stop_px,
            self.min_qty == other.min_qty,
            self.max_price_levels == other.max_price_levels,
            self.time_in_force == other.time_in_force,
            self.cash_margin == other.cash_margin
        ])
        
    


class Extend204129(BinaryCodec):
    def __init__(self):
        self.member_id = ''
        self.investor_type = ''
        self.investor_id = ''
        self.investor_name = ''
        self.trader_code = ''
        self.secondary_order_id = ''
        self.bid_trans_type = 0
        self.bid_exec_inst_type = 0
        self.low_limit_price = 0
        self.high_limit_price = 0
        self.min_qty = 0
        self.trade_date = 0
        self.settl_type = 0
        self.settl_period = 0
        self.pre_trade_anonymity = 0
        self.cash_margin = ''
        self.memo = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.member_id, 6)
        write_fixed_string(buffer, self.investor_type, 2)
        write_fixed_string(buffer, self.investor_id, 10)
        write_fixed_string(buffer, self.investor_name, 120)
        write_fixed_string(buffer, self.trader_code, 8)
        write_fixed_string(buffer, self.secondary_order_id, 16)
        buffer.write_u16(self.bid_trans_type)
        buffer.write_u16(self.bid_exec_inst_type)
        buffer.write_i64(self.low_limit_price)
        buffer.write_i64(self.high_limit_price)
        buffer.write_i64(self.min_qty)
        buffer.write_u32(self.trade_date)
        buffer.write_u16(self.settl_type)
        buffer.write_u8(self.settl_period)
        buffer.write_u8(self.pre_trade_anonymity)
        write_fixed_string(buffer, self.cash_margin, 1)
        write_fixed_string(buffer, self.memo, 160)
    
    def decode(self, buffer: ByteBuf):
        self.member_id = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.investor_type = buffer.read_bytes(2).decode('utf-8').strip('\x00')
        self.investor_id = buffer.read_bytes(10).decode('utf-8').strip('\x00')
        self.investor_name = buffer.read_bytes(120).decode('utf-8').strip('\x00')
        self.trader_code = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.secondary_order_id = buffer.read_bytes(16).decode('utf-8').strip('\x00')
        self.bid_trans_type = buffer.read_u16()
        self.bid_exec_inst_type = buffer.read_u16()
        self.low_limit_price = buffer.read_i64()
        self.high_limit_price = buffer.read_i64()
        self.min_qty = buffer.read_i64()
        self.trade_date = buffer.read_u32()
        self.settl_type = buffer.read_u16()
        self.settl_period = buffer.read_u8()
        self.pre_trade_anonymity = buffer.read_u8()
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.memo = buffer.read_bytes(160).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.member_id == other.member_id,
            self.investor_type == other.investor_type,
            self.investor_id == other.investor_id,
            self.investor_name == other.investor_name,
            self.trader_code == other.trader_code,
            self.secondary_order_id == other.secondary_order_id,
            self.bid_trans_type == other.bid_trans_type,
            self.bid_exec_inst_type == other.bid_exec_inst_type,
            self.low_limit_price == other.low_limit_price,
            self.high_limit_price == other.high_limit_price,
            self.min_qty == other.min_qty,
            self.trade_date == other.trade_date,
            self.settl_type == other.settl_type,
            self.settl_period == other.settl_period,
            self.pre_trade_anonymity == other.pre_trade_anonymity,
            self.cash_margin == other.cash_margin,
            self.memo == other.memo
        ])
        
    


class Extend204702(BinaryCodec):
    def __init__(self):
        self.secondary_order_id = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.secondary_order_id, 16)
    
    def decode(self, buffer: ByteBuf):
        self.secondary_order_id = buffer.read_bytes(16).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.secondary_order_id == other.secondary_order_id
        ])
        
    


class ExecutionReport(BinaryCodec):
    def __init__(self):
        self.partition_no = 0
        self.report_index = 0
        self.appl_id = ''
        self.reporting_pbuid = ''
        self.submitting_pbuid = ''
        self.security_id = ''
        self.security_id_source = ''
        self.owner_type = 0
        self.clearing_firm = ''
        self.transact_time = 0
        self.user_info = ''
        self.order_id = ''
        self.cl_ord_id = ''
        self.quote_msg_id = ''
        self.exec_id = ''
        self.exec_type = ''
        self.ord_status = ''
        self.last_px = 0
        self.last_qty = 0
        self.leaves_qty = 0
        self.cum_qty = 0
        self.side = ''
        self.account_id = ''
        self.branch_id = ''
        self.appl_extend = None
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i32(self.partition_no)
        buffer.write_i64(self.report_index)
        write_fixed_string(buffer, self.appl_id, 3)
        write_fixed_string(buffer, self.reporting_pbuid, 6)
        write_fixed_string(buffer, self.submitting_pbuid, 6)
        write_fixed_string(buffer, self.security_id, 8)
        write_fixed_string(buffer, self.security_id_source, 4)
        buffer.write_u16(self.owner_type)
        write_fixed_string(buffer, self.clearing_firm, 2)
        buffer.write_i64(self.transact_time)
        write_fixed_string(buffer, self.user_info, 8)
        write_fixed_string(buffer, self.order_id, 16)
        write_fixed_string(buffer, self.cl_ord_id, 10)
        write_fixed_string(buffer, self.quote_msg_id, 10)
        write_fixed_string(buffer, self.exec_id, 16)
        write_fixed_string(buffer, self.exec_type, 1)
        write_fixed_string(buffer, self.ord_status, 1)
        buffer.write_i64(self.last_px)
        buffer.write_i64(self.last_qty)
        buffer.write_i64(self.leaves_qty)
        buffer.write_i64(self.cum_qty)
        write_fixed_string(buffer, self.side, 1)
        write_fixed_string(buffer, self.account_id, 12)
        write_fixed_string(buffer, self.branch_id, 4)
        if self.appl_extend is not None:
            self.appl_extend.encode(buffer)
    
    def decode(self, buffer: ByteBuf):
        self.partition_no = buffer.read_i32()
        self.report_index = buffer.read_i64()
        self.appl_id = buffer.read_bytes(3).decode('utf-8').strip('\x00')
        self.reporting_pbuid = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.submitting_pbuid = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.security_id = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.security_id_source = buffer.read_bytes(4).decode('utf-8').strip('\x00')
        self.owner_type = buffer.read_u16()
        self.clearing_firm = buffer.read_bytes(2).decode('utf-8').strip('\x00')
        self.transact_time = buffer.read_i64()
        self.user_info = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.order_id = buffer.read_bytes(16).decode('utf-8').strip('\x00')
        self.cl_ord_id = buffer.read_bytes(10).decode('utf-8').strip('\x00')
        self.quote_msg_id = buffer.read_bytes(10).decode('utf-8').strip('\x00')
        self.exec_id = buffer.read_bytes(16).decode('utf-8').strip('\x00')
        self.exec_type = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.ord_status = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.last_px = buffer.read_i64()
        self.last_qty = buffer.read_i64()
        self.leaves_qty = buffer.read_i64()
        self.cum_qty = buffer.read_i64()
        self.side = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.account_id = buffer.read_bytes(12).decode('utf-8').strip('\x00')
        self.branch_id = buffer.read_bytes(4).decode('utf-8').strip('\x00')
        if self.appl_id == "010":
            self.appl_extend = Extend200115()
        if self.appl_id == "020":
            self.appl_extend = Extend200215()
        if self.appl_id == "030":
            self.appl_extend = Extend200315()
        if self.appl_id == "051":
            self.appl_extend = Extend200515()
        if self.appl_id == "052":
            self.appl_extend = Extend200515()
        if self.appl_id == "056":
            self.appl_extend = Extend200515()
        if self.appl_id == "057":
            self.appl_extend = Extend200515()
        if self.appl_id == "060":
            self.appl_extend = Extend200615()
        if self.appl_id == "061":
            self.appl_extend = Extend200615()
        if self.appl_id == "070":
            self.appl_extend = Extend200715()
        if self.appl_id == "630":
            self.appl_extend = Extend206315()
        if self.appl_id == "370":
            self.appl_extend = Extend203715()
        if self.appl_id == "410":
            self.appl_extend = Extend204115()
        if self.appl_id == "412":
            self.appl_extend = Extend204115()
        if self.appl_id == "413":
            self.appl_extend = Extend204115()
        if self.appl_id == "415":
            self.appl_extend = Extend204115()
        if self.appl_id == "416":
            self.appl_extend = Extend204115()
        if self.appl_id == "417":
            self.appl_extend = Extend204130()
        if self.appl_id == "470":
            self.appl_extend = Extend204715()
        self.appl_extend.decode(buffer)
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.partition_no == other.partition_no,
            self.report_index == other.report_index,
            self.appl_id == other.appl_id,
            self.reporting_pbuid == other.reporting_pbuid,
            self.submitting_pbuid == other.submitting_pbuid,
            self.security_id == other.security_id,
            self.security_id_source == other.security_id_source,
            self.owner_type == other.owner_type,
            self.clearing_firm == other.clearing_firm,
            self.transact_time == other.transact_time,
            self.user_info == other.user_info,
            self.order_id == other.order_id,
            self.cl_ord_id == other.cl_ord_id,
            self.quote_msg_id == other.quote_msg_id,
            self.exec_id == other.exec_id,
            self.exec_type == other.exec_type,
            self.ord_status == other.ord_status,
            self.last_px == other.last_px,
            self.last_qty == other.last_qty,
            self.leaves_qty == other.leaves_qty,
            self.cum_qty == other.cum_qty,
            self.side == other.side,
            self.account_id == other.account_id,
            self.branch_id == other.branch_id,
            self.appl_extend == other.appl_extend
        ])
        
    


class Extend200115(BinaryCodec):
    def __init__(self):
        self.cash_margin = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.cash_margin, 1)
    
    def decode(self, buffer: ByteBuf):
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.cash_margin == other.cash_margin
        ])
        
    


class Extend200215(BinaryCodec):
    def __init__(self):
        self.maturity_date = 0
    
    def encode(self, buffer: ByteBuf):
        buffer.write_u32(self.maturity_date)
    
    def decode(self, buffer: ByteBuf):
        self.maturity_date = buffer.read_u32()
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.maturity_date == other.maturity_date
        ])
        
    


class Extend200315(BinaryCodec):
    def __init__(self):
        self.maturity_date = 0
    
    def encode(self, buffer: ByteBuf):
        buffer.write_u32(self.maturity_date)
    
    def decode(self, buffer: ByteBuf):
        self.maturity_date = buffer.read_u32()
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.maturity_date == other.maturity_date
        ])
        
    


class Extend200415(BinaryCodec):
    def __init__(self):
        self.position_effect = ''
        self.covered_or_uncovered = 0
        self.contract_account_code = ''
        self.secondary_order_id = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.position_effect, 1)
        buffer.write_u8(self.covered_or_uncovered)
        write_fixed_string(buffer, self.contract_account_code, 6)
        write_fixed_string(buffer, self.secondary_order_id, 16)
    
    def decode(self, buffer: ByteBuf):
        self.position_effect = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.covered_or_uncovered = buffer.read_u8()
        self.contract_account_code = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.secondary_order_id = buffer.read_bytes(16).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.position_effect == other.position_effect,
            self.covered_or_uncovered == other.covered_or_uncovered,
            self.contract_account_code == other.contract_account_code,
            self.secondary_order_id == other.secondary_order_id
        ])
        
    


class Extend200515(BinaryCodec):
    def __init__(self):
        self.confirm_id = ''
        self.cash_margin = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.confirm_id, 8)
        write_fixed_string(buffer, self.cash_margin, 1)
    
    def decode(self, buffer: ByteBuf):
        self.confirm_id = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.confirm_id == other.confirm_id,
            self.cash_margin == other.cash_margin
        ])
        
    


class Extend200615(BinaryCodec):
    def __init__(self):
        self.cash_margin = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.cash_margin, 1)
    
    def decode(self, buffer: ByteBuf):
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.cash_margin == other.cash_margin
        ])
        
    


class Extend206315(BinaryCodec):
    def __init__(self):
        self.cash_margin = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.cash_margin, 1)
    
    def decode(self, buffer: ByteBuf):
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.cash_margin == other.cash_margin
        ])
        
    


class Extend200715(BinaryCodec):
    def __init__(self):
        self.expiration_days = 0
        self.expiration_type = 0
        self.maturity_date = 0
        self.share_property = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_u16(self.expiration_days)
        buffer.write_u8(self.expiration_type)
        buffer.write_u32(self.maturity_date)
        write_fixed_string(buffer, self.share_property, 2)
    
    def decode(self, buffer: ByteBuf):
        self.expiration_days = buffer.read_u16()
        self.expiration_type = buffer.read_u8()
        self.maturity_date = buffer.read_u32()
        self.share_property = buffer.read_bytes(2).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.expiration_days == other.expiration_days,
            self.expiration_type == other.expiration_type,
            self.maturity_date == other.maturity_date,
            self.share_property == other.share_property
        ])
        
    


class Extend204715(BinaryCodec):
    def __init__(self):
        self.expiration_days = 0
        self.expiration_type = 0
        self.maturity_date = 0
        self.share_property = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_u16(self.expiration_days)
        buffer.write_u8(self.expiration_type)
        buffer.write_u32(self.maturity_date)
        write_fixed_string(buffer, self.share_property, 2)
    
    def decode(self, buffer: ByteBuf):
        self.expiration_days = buffer.read_u16()
        self.expiration_type = buffer.read_u8()
        self.maturity_date = buffer.read_u32()
        self.share_property = buffer.read_bytes(2).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.expiration_days == other.expiration_days,
            self.expiration_type == other.expiration_type,
            self.maturity_date == other.maturity_date,
            self.share_property == other.share_property
        ])
        
    


class Extend203715(BinaryCodec):
    def __init__(self):
        self.cash_margin = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.cash_margin, 1)
    
    def decode(self, buffer: ByteBuf):
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.cash_margin == other.cash_margin
        ])
        
    


class Extend204115(BinaryCodec):
    def __init__(self):
        self.cash_margin = ''
        self.settl_type = 0
        self.settl_period = 0
        self.counterparty_member_id = ''
        self.counterparty_investor_type = ''
        self.counterparty_investor_id = ''
        self.counterparty_investor_name = ''
        self.counterparty_trader_code = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.cash_margin, 1)
        buffer.write_u16(self.settl_type)
        buffer.write_u8(self.settl_period)
        write_fixed_string(buffer, self.counterparty_member_id, 6)
        write_fixed_string(buffer, self.counterparty_investor_type, 2)
        write_fixed_string(buffer, self.counterparty_investor_id, 10)
        write_fixed_string(buffer, self.counterparty_investor_name, 120)
        write_fixed_string(buffer, self.counterparty_trader_code, 8)
    
    def decode(self, buffer: ByteBuf):
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.settl_type = buffer.read_u16()
        self.settl_period = buffer.read_u8()
        self.counterparty_member_id = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.counterparty_investor_type = buffer.read_bytes(2).decode('utf-8').strip('\x00')
        self.counterparty_investor_id = buffer.read_bytes(10).decode('utf-8').strip('\x00')
        self.counterparty_investor_name = buffer.read_bytes(120).decode('utf-8').strip('\x00')
        self.counterparty_trader_code = buffer.read_bytes(8).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.cash_margin == other.cash_margin,
            self.settl_type == other.settl_type,
            self.settl_period == other.settl_period,
            self.counterparty_member_id == other.counterparty_member_id,
            self.counterparty_investor_type == other.counterparty_investor_type,
            self.counterparty_investor_id == other.counterparty_investor_id,
            self.counterparty_investor_name == other.counterparty_investor_name,
            self.counterparty_trader_code == other.counterparty_trader_code
        ])
        
    


class Extend204130(BinaryCodec):
    def __init__(self):
        self.member_id = ''
        self.investor_type = ''
        self.investor_id = ''
        self.investor_name = ''
        self.trader_code = ''
        self.counterparty_member_id = ''
        self.counterparty_investor_type = ''
        self.counterparty_investor_id = ''
        self.counterparty_investor_name = ''
        self.counterparty_trader_code = ''
        self.secondary_order_id = ''
        self.bid_trans_type = 0
        self.bid_exec_inst_type = 0
        self.settl_type = 0
        self.settl_period = 0
        self.cash_margin = ''
        self.memo = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.member_id, 6)
        write_fixed_string(buffer, self.investor_type, 2)
        write_fixed_string(buffer, self.investor_id, 10)
        write_fixed_string(buffer, self.investor_name, 120)
        write_fixed_string(buffer, self.trader_code, 8)
        write_fixed_string(buffer, self.counterparty_member_id, 6)
        write_fixed_string(buffer, self.counterparty_investor_type, 2)
        write_fixed_string(buffer, self.counterparty_investor_id, 10)
        write_fixed_string(buffer, self.counterparty_investor_name, 120)
        write_fixed_string(buffer, self.counterparty_trader_code, 8)
        write_fixed_string(buffer, self.secondary_order_id, 16)
        buffer.write_u16(self.bid_trans_type)
        buffer.write_u16(self.bid_exec_inst_type)
        buffer.write_u16(self.settl_type)
        buffer.write_u8(self.settl_period)
        write_fixed_string(buffer, self.cash_margin, 1)
        write_fixed_string(buffer, self.memo, 160)
    
    def decode(self, buffer: ByteBuf):
        self.member_id = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.investor_type = buffer.read_bytes(2).decode('utf-8').strip('\x00')
        self.investor_id = buffer.read_bytes(10).decode('utf-8').strip('\x00')
        self.investor_name = buffer.read_bytes(120).decode('utf-8').strip('\x00')
        self.trader_code = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.counterparty_member_id = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.counterparty_investor_type = buffer.read_bytes(2).decode('utf-8').strip('\x00')
        self.counterparty_investor_id = buffer.read_bytes(10).decode('utf-8').strip('\x00')
        self.counterparty_investor_name = buffer.read_bytes(120).decode('utf-8').strip('\x00')
        self.counterparty_trader_code = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.secondary_order_id = buffer.read_bytes(16).decode('utf-8').strip('\x00')
        self.bid_trans_type = buffer.read_u16()
        self.bid_exec_inst_type = buffer.read_u16()
        self.settl_type = buffer.read_u16()
        self.settl_period = buffer.read_u8()
        self.cash_margin = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.memo = buffer.read_bytes(160).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.member_id == other.member_id,
            self.investor_type == other.investor_type,
            self.investor_id == other.investor_id,
            self.investor_name == other.investor_name,
            self.trader_code == other.trader_code,
            self.counterparty_member_id == other.counterparty_member_id,
            self.counterparty_investor_type == other.counterparty_investor_type,
            self.counterparty_investor_id == other.counterparty_investor_id,
            self.counterparty_investor_name == other.counterparty_investor_name,
            self.counterparty_trader_code == other.counterparty_trader_code,
            self.secondary_order_id == other.secondary_order_id,
            self.bid_trans_type == other.bid_trans_type,
            self.bid_exec_inst_type == other.bid_exec_inst_type,
            self.settl_type == other.settl_type,
            self.settl_period == other.settl_period,
            self.cash_margin == other.cash_margin,
            self.memo == other.memo
        ])
        
    


class OrderCancelRequest(BinaryCodec):
    def __init__(self):
        self.appl_id = ''
        self.submitting_pbuid = ''
        self.security_id = ''
        self.security_id_source = ''
        self.owner_type = 0
        self.clearing_firm = ''
        self.transact_time = 0
        self.user_info = ''
        self.cl_ord_id = ''
        self.orig_cl_ord_id = ''
        self.side = ''
        self.order_id = ''
        self.order_qty = 0
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.appl_id, 3)
        write_fixed_string(buffer, self.submitting_pbuid, 6)
        write_fixed_string(buffer, self.security_id, 8)
        write_fixed_string(buffer, self.security_id_source, 4)
        buffer.write_u16(self.owner_type)
        write_fixed_string(buffer, self.clearing_firm, 2)
        buffer.write_i64(self.transact_time)
        write_fixed_string(buffer, self.user_info, 8)
        write_fixed_string(buffer, self.cl_ord_id, 10)
        write_fixed_string(buffer, self.orig_cl_ord_id, 10)
        write_fixed_string(buffer, self.side, 1)
        write_fixed_string(buffer, self.order_id, 16)
        buffer.write_i64(self.order_qty)
    
    def decode(self, buffer: ByteBuf):
        self.appl_id = buffer.read_bytes(3).decode('utf-8').strip('\x00')
        self.submitting_pbuid = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.security_id = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.security_id_source = buffer.read_bytes(4).decode('utf-8').strip('\x00')
        self.owner_type = buffer.read_u16()
        self.clearing_firm = buffer.read_bytes(2).decode('utf-8').strip('\x00')
        self.transact_time = buffer.read_i64()
        self.user_info = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.cl_ord_id = buffer.read_bytes(10).decode('utf-8').strip('\x00')
        self.orig_cl_ord_id = buffer.read_bytes(10).decode('utf-8').strip('\x00')
        self.side = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.order_id = buffer.read_bytes(16).decode('utf-8').strip('\x00')
        self.order_qty = buffer.read_i64()
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.appl_id == other.appl_id,
            self.submitting_pbuid == other.submitting_pbuid,
            self.security_id == other.security_id,
            self.security_id_source == other.security_id_source,
            self.owner_type == other.owner_type,
            self.clearing_firm == other.clearing_firm,
            self.transact_time == other.transact_time,
            self.user_info == other.user_info,
            self.cl_ord_id == other.cl_ord_id,
            self.orig_cl_ord_id == other.orig_cl_ord_id,
            self.side == other.side,
            self.order_id == other.order_id,
            self.order_qty == other.order_qty
        ])
        
    


class CancelReject(BinaryCodec):
    def __init__(self):
        self.partition_no = 0
        self.report_index = 0
        self.appl_id = ''
        self.reporting_pbuid = ''
        self.submitting_pbuid = ''
        self.security_id = ''
        self.security_id_source = ''
        self.owner_type = 0
        self.clearing_firm = ''
        self.transact_time = 0
        self.user_info = ''
        self.cl_ord_id = ''
        self.orig_cl_ord_id = ''
        self.side = ''
        self.ord_status = ''
        self.cxl_rej_reason = 0
        self.reject_text = ''
        self.order_id = ''
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i32(self.partition_no)
        buffer.write_i64(self.report_index)
        write_fixed_string(buffer, self.appl_id, 3)
        write_fixed_string(buffer, self.reporting_pbuid, 6)
        write_fixed_string(buffer, self.submitting_pbuid, 6)
        write_fixed_string(buffer, self.security_id, 8)
        write_fixed_string(buffer, self.security_id_source, 4)
        buffer.write_u16(self.owner_type)
        write_fixed_string(buffer, self.clearing_firm, 2)
        buffer.write_i64(self.transact_time)
        write_fixed_string(buffer, self.user_info, 8)
        write_fixed_string(buffer, self.cl_ord_id, 10)
        write_fixed_string(buffer, self.orig_cl_ord_id, 10)
        write_fixed_string(buffer, self.side, 1)
        write_fixed_string(buffer, self.ord_status, 1)
        buffer.write_u16(self.cxl_rej_reason)
        write_fixed_string(buffer, self.reject_text, 16)
        write_fixed_string(buffer, self.order_id, 16)
    
    def decode(self, buffer: ByteBuf):
        self.partition_no = buffer.read_i32()
        self.report_index = buffer.read_i64()
        self.appl_id = buffer.read_bytes(3).decode('utf-8').strip('\x00')
        self.reporting_pbuid = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.submitting_pbuid = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.security_id = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.security_id_source = buffer.read_bytes(4).decode('utf-8').strip('\x00')
        self.owner_type = buffer.read_u16()
        self.clearing_firm = buffer.read_bytes(2).decode('utf-8').strip('\x00')
        self.transact_time = buffer.read_i64()
        self.user_info = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.cl_ord_id = buffer.read_bytes(10).decode('utf-8').strip('\x00')
        self.orig_cl_ord_id = buffer.read_bytes(10).decode('utf-8').strip('\x00')
        self.side = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.ord_status = buffer.read_bytes(1).decode('utf-8').strip('\x00')
        self.cxl_rej_reason = buffer.read_u16()
        self.reject_text = buffer.read_bytes(16).decode('utf-8').strip('\x00')
        self.order_id = buffer.read_bytes(16).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.partition_no == other.partition_no,
            self.report_index == other.report_index,
            self.appl_id == other.appl_id,
            self.reporting_pbuid == other.reporting_pbuid,
            self.submitting_pbuid == other.submitting_pbuid,
            self.security_id == other.security_id,
            self.security_id_source == other.security_id_source,
            self.owner_type == other.owner_type,
            self.clearing_firm == other.clearing_firm,
            self.transact_time == other.transact_time,
            self.user_info == other.user_info,
            self.cl_ord_id == other.cl_ord_id,
            self.orig_cl_ord_id == other.orig_cl_ord_id,
            self.side == other.side,
            self.ord_status == other.ord_status,
            self.cxl_rej_reason == other.cxl_rej_reason,
            self.reject_text == other.reject_text,
            self.order_id == other.order_id
        ])
        
    


class BusinessReject(BinaryCodec):
    def __init__(self):
        self.appl_id = ''
        self.transact_time = 0
        self.submitting_pbuid = ''
        self.security_id = ''
        self.security_id_source = ''
        self.ref_seq_num = 0
        self.ref_msg_type = 0
        self.business_reject_ref_id = ''
        self.business_reject_reason = 0
        self.business_reject_text = ''
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.appl_id, 3)
        buffer.write_i64(self.transact_time)
        write_fixed_string(buffer, self.submitting_pbuid, 6)
        write_fixed_string(buffer, self.security_id, 8)
        write_fixed_string(buffer, self.security_id_source, 4)
        buffer.write_i64(self.ref_seq_num)
        buffer.write_u32(self.ref_msg_type)
        write_fixed_string(buffer, self.business_reject_ref_id, 10)
        buffer.write_u16(self.business_reject_reason)
        write_fixed_string(buffer, self.business_reject_text, 50)
    
    def decode(self, buffer: ByteBuf):
        self.appl_id = buffer.read_bytes(3).decode('utf-8').strip('\x00')
        self.transact_time = buffer.read_i64()
        self.submitting_pbuid = buffer.read_bytes(6).decode('utf-8').strip('\x00')
        self.security_id = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.security_id_source = buffer.read_bytes(4).decode('utf-8').strip('\x00')
        self.ref_seq_num = buffer.read_i64()
        self.ref_msg_type = buffer.read_u32()
        self.business_reject_ref_id = buffer.read_bytes(10).decode('utf-8').strip('\x00')
        self.business_reject_reason = buffer.read_u16()
        self.business_reject_text = buffer.read_bytes(50).decode('utf-8').strip('\x00')
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.appl_id == other.appl_id,
            self.transact_time == other.transact_time,
            self.submitting_pbuid == other.submitting_pbuid,
            self.security_id == other.security_id,
            self.security_id_source == other.security_id_source,
            self.ref_seq_num == other.ref_seq_num,
            self.ref_msg_type == other.ref_msg_type,
            self.business_reject_ref_id == other.business_reject_ref_id,
            self.business_reject_reason == other.business_reject_reason,
            self.business_reject_text == other.business_reject_text
        ])
        
    


class ReportSynchronization(BinaryCodec):
    def __init__(self):
        self.partition_report = []
    
    def encode(self, buffer: ByteBuf):
        size = len(self.partition_report)
        buffer.write_u32(size)
        for i in range(size):
            self.partition_report[i].encode(buffer)
        
    
    def decode(self, buffer: ByteBuf):
        size = get_len(buffer, 'u32')
        for i in range(size):
            _partition_report = PartitionReport()
            _partition_report.decode(buffer)
            self.partition_report.append(_partition_report)
        
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.partition_report == other.partition_report
        ])
        
    


class PartitionReport(BinaryCodec):
    def __init__(self):
        self.partition_no = 0
        self.report_index = 0
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i32(self.partition_no)
        buffer.write_i64(self.report_index)
    
    def decode(self, buffer: ByteBuf):
        self.partition_no = buffer.read_i32()
        self.report_index = buffer.read_i64()
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.partition_no == other.partition_no,
            self.report_index == other.report_index
        ])
        
    


class PlatformStateInfo(BinaryCodec):
    def __init__(self):
        self.platform_id = 0
        self.platform_state = 0
    
    def encode(self, buffer: ByteBuf):
        buffer.write_u16(self.platform_id)
        buffer.write_u16(self.platform_state)
    
    def decode(self, buffer: ByteBuf):
        self.platform_id = buffer.read_u16()
        self.platform_state = buffer.read_u16()
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.platform_id == other.platform_id,
            self.platform_state == other.platform_state
        ])
        
    


class ReportFinished(BinaryCodec):
    def __init__(self):
        self.partition_no = 0
        self.report_index = 0
        self.platform_id = 0
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i32(self.partition_no)
        buffer.write_i64(self.report_index)
        buffer.write_u16(self.platform_id)
    
    def decode(self, buffer: ByteBuf):
        self.partition_no = buffer.read_i32()
        self.report_index = buffer.read_i64()
        self.platform_id = buffer.read_u16()
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.partition_no == other.partition_no,
            self.report_index == other.report_index,
            self.platform_id == other.platform_id
        ])
        
    


class PlatformInfo(BinaryCodec):
    def __init__(self):
        self.platform_id = 0
        self.platform_partition = []
    
    def encode(self, buffer: ByteBuf):
        buffer.write_u16(self.platform_id)
        size = len(self.platform_partition)
        buffer.write_u32(size)
        for i in range(size):
            self.platform_partition[i].encode(buffer)
        
    
    def decode(self, buffer: ByteBuf):
        self.platform_id = buffer.read_u16()
        size = get_len(buffer, 'u32')
        for i in range(size):
            _platform_partition = PlatformPartition()
            _platform_partition.decode(buffer)
            self.platform_partition.append(_platform_partition)
        
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.platform_id == other.platform_id,
            self.platform_partition == other.platform_partition
        ])
        
    


class PlatformPartition(BinaryCodec):
    def __init__(self):
        self.partition_no = 0
    
    def encode(self, buffer: ByteBuf):
        buffer.write_i32(self.partition_no)
    
    def decode(self, buffer: ByteBuf):
        self.partition_no = buffer.read_i32()
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.partition_no == other.partition_no
        ])
        
    


class TradingSessionStatus(BinaryCodec):
    def __init__(self):
        self.market_id = ''
        self.market_segment_id = ''
        self.trading_session_id = ''
        self.trading_session_sub_id = ''
        self.trad_ses_status = 0
        self.trad_ses_start_time = 0
        self.trad_ses_end_time = 0
    
    def encode(self, buffer: ByteBuf):
        write_fixed_string(buffer, self.market_id, 8)
        write_fixed_string(buffer, self.market_segment_id, 8)
        write_fixed_string(buffer, self.trading_session_id, 4)
        write_fixed_string(buffer, self.trading_session_sub_id, 4)
        buffer.write_u16(self.trad_ses_status)
        buffer.write_i64(self.trad_ses_start_time)
        buffer.write_i64(self.trad_ses_end_time)
    
    def decode(self, buffer: ByteBuf):
        self.market_id = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.market_segment_id = buffer.read_bytes(8).decode('utf-8').strip('\x00')
        self.trading_session_id = buffer.read_bytes(4).decode('utf-8').strip('\x00')
        self.trading_session_sub_id = buffer.read_bytes(4).decode('utf-8').strip('\x00')
        self.trad_ses_status = buffer.read_u16()
        self.trad_ses_start_time = buffer.read_i64()
        self.trad_ses_end_time = buffer.read_i64()
    
    def __eq__(self, other):
        if not isinstance(other, self.__class__):
            return False
        return all([
            self.market_id == other.market_id,
            self.market_segment_id == other.market_segment_id,
            self.trading_session_id == other.trading_session_id,
            self.trading_session_sub_id == other.trading_session_sub_id,
            self.trad_ses_status == other.trad_ses_status,
            self.trad_ses_start_time == other.trad_ses_start_time,
            self.trad_ses_end_time == other.trad_ses_end_time
        ])
        
    


